import { describe, it } from 'vitest';

describe('env config', () => {
  it.todo('accepts a valid environment and returns typed config [M0-AC-05]');
  it.todo('rejects a missing required variable and names it [M0-AC-05]');
  it.todo('rejects a malformed variable and names it [M0-AC-05]');
});
